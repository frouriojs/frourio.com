---
id: gui
title: GUI によるセットアップ
---

## 1. create-frourio-app GUI の起動 {#start-cfa-gui}

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import BrowserWindow from '@site/src/components/BrowserWindow';

<Tabs
  defaultValue="npx"
  values={[
    { label: 'npx', value: 'npx' },
    { label: 'npm', value: 'npm' },
    { label: 'yarn', value: 'yarn' },
  ]
}>
<TabItem value="npx">

```sh
npx create-frourio-app
```

</TabItem>
<TabItem value="npm">

```sh
npm init frourio-app
```

</TabItem>
<TabItem value="yarn">

```sh
yarn create frourio-app
```

</TabItem>
</Tabs>

コマンドを実行すると、create-frourio-app が開始し [localhost:3000](http://localhost:3000) がブラウザで開かれます。

<BrowserWindow url="http://localhost:3000">
  <img src="/img/cfa-gui.png" />
</BrowserWindow>

## 2. Frourio プロジェクトの生成 {#2-generate-frourio-project}

空欄を埋め、選択肢を選び終えたら、「Create」ボタンを押して frourio プロジェクトの生成を開始します。

生成が完了すると、[localhost:8000](http://localhost:8000) がブラウザで開かれ、選択したクライアントフレームワークが表示されます。
（もし自動で開かない場合は、[localhost:8000](http://localhost:8000) を手動で開いてください。）

:::caution

もし別のアプリケーションが 8000 番ポートを使用している場合は、おそらく起動に失敗します。
そのような場合でも生成は完了しているので、frourio アプリケーションを終了してもう一度手動で開始してください（例: `npm run dev`）。

:::

<Tabs
  defaultValue="next"
  values={[
    { label: 'Next.js', value: 'next' },
    { label: 'Nuxt.js', value: 'nuxt' },
    { label: 'Sapper', value: 'sapper' },
  ]}
>
  <TabItem value="next">
    <BrowserWindow url="http://localhost:8000">
      <img src="/img/cfa-done-next.png" />
    </BrowserWindow>
  </TabItem>
  <TabItem value="nuxt">
    <BrowserWindow url="http://localhost:8000">
      <img src="/img/cfa-done-nuxt.png" />
    </BrowserWindow>
  </TabItem>
  <TabItem value="sapper">
    <BrowserWindow url="http://localhost:8000">
      <img src="/img/cfa-done-sapper.png" />
    </BrowserWindow>
  </TabItem>
</Tabs>
