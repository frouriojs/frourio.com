---
id: controller
title: Controller
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

```ts title='controller.ts'
import { defineController } from './$relay';

export default defineController(() => ({
  get: () => ({ status: 200, body: 'Hello' }),
  post: async ({ query: { foo }, params: { bar }, body }) => {
    const baz = await createBaz(foo, bar, body);
    if (!baz) return { status: 400 };
    return { status: 201, body: baz };
  },
}));
```

To define controllers, use the function `defineMethods` exported by `./$relay.ts`.

The type definition of defineController is automatically generated based on the aspida type definition. It must be imported from `$relay.ts` in the same directory as the controller.

The return value of defineController must be default exported.

If you want to use the fastify instance in the controller, you can take it as an argument.

## Function `defineController` {#function-definecontroller}

### Argument Type {#definecontoller-argument-type}

<Tabs
  groupId='base-framework'
  defaultValue="fastify"
  values={[
    { label: 'Fastify', value: 'fastify' },
    { label: 'Express', value: 'express' },
  ]}
>
<TabItem value="fastify">

- function `(fastify: FastifyInstance) => ServerMethods`
  - [`FastifyInstance`](https://www.fastify.io/docs/latest/Reference/TypeScript/#fastifyfastifyinstancerawserver-rawrequest-requestgeneric-logger)
  - `ServerMethods` : an object whose keys are the same as `Methods` and whose values are [ServerMethodHandler](#object-servermethodhandler).

</TabItem>
<TabItem value="express">

- function `(app: Express) => ControllerMethods`
  - [`Express`](https://expressjs.com/en/4x/api.html)
  - `ServerMethods` : an object whose keys are the same as `Methods` and whose values are [ServerMethodHandler](#object-servermethodhandler).

</TabItem>
</Tabs>

### Return Type {#definecontoller-return-type}

- object `ServerMethods`

## Object `ServerMethodHandler` {#object-servermethodhandler}

```ts title='$server.ts (formatted)'
export type ServerMethodHandler<T extends AspidaMethodParams, U extends Record<string, any> = {}> =
  | ServerHandler<T, U>
  | ServerHandlerPromise<T, U>
  | {
      validators?: Partial<{ [Key in keyof RequestParams<T>]?: z.ZodType<RequestParams<T>[Key]> }>;
      handler: ServerHandler<T, U> | ServerHandlerPromise<T, U>;
    };
```

- [`ServerHandler`](#function-serverhandler) or
- <code>
    <a href="#function-serverhandler">ServerHandler</a>Promise
  </code> or
- <code>
    {'{ validators?: '}
    <a href="/docs/reference/validation/zod#controller-level">(Controller-level Validators)</a>
    {', handler: '}
    <a href="#function-serverhandler">ServerHandler</a>
    {' }'}
  </code> or
- <code>
    {'{ validators?: '}
    <a href="/docs/reference/validation/zod#controller-level">(Controller-level Validators)</a>
    {', handler: '}
    <a href="#function-serverhandler">ServerHandler</a>Promise
    {' }'}
  </code>

## Function `ServerHandler` {#function-serverhandler}

### Argument Type {#serverhandler-argument-type}

- object `RequestParams` & [`AdditinalRequest`](/docs/reference/additionalRequest)
  - `query` : `query`
  - `headers` : `reqHeaders`
  - `params` : URL Params
  - `body` : `reqBody`

### Return Type {#controllermethods-return-type}

- object `ServerResponse` | promise `Promise<ServerResponse>`
  - `status` : `status`
  - `body` : `resBody`
  - `headers` : `resHeaders`

The types of `RequestParams` and `ServerResponse` are determined by API type definitions.

If this is included in a Promise using async, it becomes the <code>ServerHandler<b>Promise</b></code>.
